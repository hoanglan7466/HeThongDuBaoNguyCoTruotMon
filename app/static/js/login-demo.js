document.addEventListener('DOMContentLoaded',()=>{const updateDate=()=>{const now=new Date(),parts=new Intl.DateTimeFormat('vi-VN',{day:'2-digit',month:'2-digit',year:'numeric'}).formatToParts(now),part=type=>parts.find(item=>item.type===type)?.value;document.querySelector('#login-date').textContent=`Hà Nội, ngày ${part('day')} tháng ${part('month')} năm ${part('year')}`};updateDate();setInterval(updateDate,60000)});
